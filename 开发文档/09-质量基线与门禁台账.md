# 09 - 质量基线与门禁台账

本文件是**带日期的权威质量记录**。凡涉及"当前基线/当前测试数/已合并"的表述，一律以本台账为准，避免 README 与各阶段文档数字漂移。

## 1. 如何验证门禁

在仓库根目录执行：

```bash
uv run pytest -q                          # 全量测试
uv run ruff check src                      # lint（门禁仅覆盖 src）
uv run ruff format --check src             # 格式化
uv run pre-commit run --all-files           # 本地钩子（ruff + pytest）
```

CI 在 `main` 与每个 PR 上强制执行前三条（见 `.github/workflows/ci.yml`）。

## 2. 质量台账

**当前 `main` HEAD**：以 `git log -1 --format=%h` 为准，不在此硬编码（避免每次合并后过期）。测试数是权威可校验数字，每次合并后以 `uv run pytest -q` 实测回填；本文件不逐行记 commit hash，避免漂移。

| 日期 | 测试数 | 说明 |
|---|---|---|
| 2026-09-22 | 322 | F-01 presale-eval 评估框架入口：4 种 mode + summarize + build_retriever（PR #105） |

### 历史基线
- 2026-09-22 | 318 | D-01 ReviewAnalyzerAgent：情感分析 + 关键词提取 + 8 个测试（PR #101） |

### 历史基线
- 2026-09-21 | 307 | E-01 真实 LLM 集成验证：create_openai_agent 组装 + 空响应/畸形/异常 4 个测试（PR #89）

### 历史基线
- 2026-09-21 | 303 | B6 业务 Agent 集成验证：PresaleAgent + B5 LoopStrategy（SinglePassLoop/RetryOnLowEvidenceLoop）7 个集成测试（PR #80）

### 历史基线
- 2026-09-21 | 296 | B5 Loop 引擎：LoopStrategy Protocol、SinglePassLoop、StepContext、Harness 多步循环、RetryOnLowEvidenceLoop（PR #75/#76/#77）

### 历史基线
- 2026-09-20 | 273 | 百级真实查询压测：22 商品/211 段落、112 条 LLM 生成真实买家口吻 golden。初跑 MRR=0.936/hit@1=0.893（recall=2/misrank=10），证明手写 golden 的满分不泛化；概念级意图覆盖 + 1 处多答案 golden 修复后 112 条 MRR/hit@1=1.0。SQLite e2e 已在上一基线落地；snapshot/README 更新，并注明需 out-of-sample 查询防止反向记忆

> 记录规则：每次合并到 `main` 后，用实测测试数更新**顶行**；顶行只保留最近一次，更早的移入「历史基线」。
>
> **已知例外**：2026-09-13 PR #9/#10 因免费层 runner 排队过久、在 CI 尚未跑完时被合并（事后 CI 均通过），违反"合并前等 CI 通过"纪律。已登记，后续不得再以排队为由跳过等待。

### 历史基线
- 2026-09-18 | 251 | #48 可复现外部环境栈（docker-compose+Makefile+deploy/README）；#49 真实 Milvus Hybrid e2e（Milvus 本地文件存储、VARCHAR 主键 schema、扁平 row）；#50 tests/ ruff 清理（27 文件，E501/F401/I001/F841）；#51 CI 真机集成 job（tests/b1/test_milvus_integration.py，默认 3 例 skip，`MILVUS_INTEGRATION=1` 时对真实 Milvus 跑索引+检索+租户隔离）。默认测试数保持 251。

### 历史基线
- 2026-09-18 | 249 | 外部检索修复：Qdrant UUID point id、确定性 embedding 兜底（`PRESALE_EMBEDDING=deterministic`）；真实 Qdrant 端到端已验证（租户隔离，catalog-001 检索成功）

### 历史基线
- 2026-09-18 | 247 | 外部检索（Qdrant 本地 + LLM embedding）：embedding/Qdrant transport/index_sources/presale-index 脚本 + 离线测试

### 历史基线
- 2026-09-17 | 243 | QA 服务可观测性：请求/终态/错误计数、`/metrics` 端点、运行日志
- 2026-09-17 | 242 | QA 服务可部署入口：`presale-qa-api`（uvicorn）+ 单例 runner；A 密钥卫生/.gitignore；QA 服务端点

### 历史基线
- 2026-09-17 | 240 | 外部检索 env 装配；pyright 进 CI（job 名 lint+format+typecheck+test，分支保护 required check 同步）

### 历史基线
- 2026-09-16 | 238 | 真实 LLM 装配：`create_openai_agent()` + env 驱动 `create_sqlite`（PR #37/#38）；数据外发边界确认
- 2026-09-16 | 238 | B4–B6 T05 完成；retro 落地：pyright（本地钩子）、CODING_STANDARDS.md、AGENTS.md
- 2026-09-16 | 234 | B4–B6 T04：Harness SQLite 跨实例重放一致（同答案/步骤/ToolCall）；accept 与技术终态分离
- 2026-09-16 | 232 | B4–B6 T03：Loop 支持 continue/finalize/need_human，Harness 多步循环受 max_steps=5 有界
- 2026-09-16 | 228 | B4–B6 T02：PresaleAgent 记录 retrieve ToolCall（工具名/租户/商品/结果状态），AgentStep 可观测
- 2026-09-16 | 226 | B4–B6 T01：可复用 agent_runtime（Harness 单步驱动售前 QA、Loop 终态、AgentOutcome）+ PresaleAgent 适配器 + 契约测试
- 2026-09-16 | 222 | 外部适配器 Slice3：新增 ExternalRetrieval(RetrievalPort)，可注入 transport/query/fallback，失败→RetrievalError 或降级标记，跨租户证据过滤，离线契约测试 + 接入 runner
- 2026-09-16 | 214 | 外部适配器 Slice2：新增 OpenAICompatibleGenerator(GeneratorPort)，可注入 transport/prompt、httpx 运行时依赖、离线契约测试 + 端到端接入 runner
- 2026-09-15 | 206 | 外部适配器阶段 Slice1：抽象 RetrievalPort/GeneratorPort + 现有实现子类化 + runner 支持 retriever 注入 + 契约测试
- 2026-09-15 | 202 | 固定 run-id + in_progress 且有 draft 改为重放（消除重复生成）；replay 异常宽度对齐（trace 读改 except Exception）；补非 TraceError/双重故障测试
- 2026-09-15 | 200 | 锁定固定 agent_run_id+in_progress 重生成行为；补 question/trace.start/evidence 持久化失败点测试；补 API 空库归档测试
- 2026-09-15 | 195 | 回退 R8 无效矩阵用例（failed_*/succeeded_unattached 冗余）；replay 的 get_by_run 包 try/except 防裸传播 + 测试
- 2026-09-14 | 197 | 修 replay 分支 trace.get 裸传播（包 try→QaRuntimeError）；修 sqlite claim 的 `return existing` 死代码；矩阵补 succeeded/failed×unattached、failed×无draft；清重复 claim_barrier 用例
- 2026-09-14 | 195 | 重放终结以 trace.answer_draft_id 为门槛（修 R6 over-termination）；幂等状态机矩阵化测试；SQLite IntegrityError 救回经 claim_barrier 测试钩子确定性覆盖
- 2026-09-13 | 183 | runner post-persist 误写 succeeded 修复（completed 不变量）+ 幂等回放测试；台账 HEAD 结构去硬编码；retention 时区/空 stages 防御；CLI --db 边界；坏 JSON→400/to_thread 测试
- 2026-09-13 | 176 | 修调度文档（cron/白名单）+ runner 收尾一致性 + API 错误映射 4xx；补 2 测试
- 2026-09-13 | 176 | 台账/README 基线同步（docs-only）
- 2026-09-13 | 174 | 维护端点路由同步化 + `PRESALE_DB_DIR` 白名单；补 3 类缺口测试
- 2026-09-13 | 174 | 台账/README 基线同步（docs-only）
- 2026-09-13 | 169 | 接入真实调度（维护端点 + 共享 helper）；补 CONTRIBUTING 与质量台账
- 2026-09-12 | 166 | 接线 30 天保留到 CLI；retention 测试重构（端口注入 + SQLite）；补齐 6 个死分支测试
- 2026-09-12 | 154 | H1 并发幂等守卫恢复 + M1 失败清理统一；引入 GitHub Actions CI
- 2026-09-12 之前：README 曾写 52/117/108 等过期数字，均以本台账与实测为准失效。

## 3. 后台维护 / 调度缝

30 天保留归档 `RetentionService.archive_expired` 提供**真实可调度**的两个入口，复用
`src/presale/maintenance.py`（CLI 走 async facade `archive_expired_sqlite`，HTTP 端点直接调用同步
`archive_expired_sqlite_blocking`，行为一致）：

**HTTP 端点**（供外部调度器触发，`database` 必须位于 `PRESALE_DB_DIR` 白名单根目录下）：

```http
POST /api/v1/presale/maintenance/retention/archive?tenant_id=<租户>&database=<sqlite路径>
POST /api/v1/presale/maintenance/health                      # 存活探针
```

`database` 查询参数默认 `presale.sqlite3`，且会被限制在环境变量 `PRESALE_DB_DIR`（默认当前工作目录）
解析后的根目录之内；越界返回 400。启动/调度前先设置它：

```bash
export PRESALE_DB_DIR=/var/lib/presale
```

Cron 约定示例（每日一次）：

```bash
# 每天 02:00 对 tenant-demo 执行保留归档（database 在 PRESALE_DB_DIR 根目录下）
PRESALE_DB_DIR=/var/lib/presale
0 2 * * * curl -sS -X POST \
  "http://localhost:8000/api/v1/presale/maintenance/retention/archive?tenant_id=tenant-demo&database=/var/lib/presale/presale.sqlite3"
```

**CLI**（适合脚本/手动）：

```bash
uv run python -m presale.cli --archive-expired --db <sqlite路径> --tenant <租户>
```

保留语义：仅归档 `COMPLETE` 且超过 30 天、未归档的 trace；历史 stages 保留，不物理删除；跨租户由 `tenant_id` 隔离。

## 4. 已知限制与待办

- **main 分支保护（已启用强制，2026-09-15）**：仓库已转为公开，`main` 已启用 branch protection——直接推送/强制推送/删除分支被禁止（`enforce_admins` 对管理员同样生效）、要求走 PR、要求 `lint + format + test` CI 通过、强制线性历史。合并纪律由此从"人工核对"升级为"平台强制"。
- **V1 售前 QA 切片收敛基线**：截至 `b2945b1`（2026-09-15），V1 presale 只读问答切片经多轮审计-修复已稳定收敛（`202 passed`、无未覆盖重要分支、纪律连续合规）。后续进入外部适配器阶段。
- **B4–B6**：Harness、Loop、业务 Agent 尚未实现（见 README 阶段表）。
- **SQLite 幂等 claim 的 `IntegrityError` 真并发救回**：SELECT 与 INSERT 之间的 Unique 冲突救回（含 failed 递归重claim）已通过 `SQLiteIdempotencyRepository` 的 `claim_barrier` 测试钩子**确定性覆盖**（见 `test_idempotency_repository.py` 三个用例：同 digest 恢复 / 异 digest 冲突 / failed 递归替换）。该钩子仅测试用、默认 no-op。
- **固定 `agent_run_id` + in_progress claim**：若该 run 已有 durable draft，runner 会先重放，并在 trace 已 attach 时终结 succeeded，避免重复生成（见 `test_fixed_agent_run_id_with_in_progress_and_draft_replays`）。若没有 draft，仍无法区分 fresh claim 与崩溃残留，按 fresh 路径重试；默认生产路径使用唯一 run_id。
- chain 任务：新增后台周期任务时应复用 `maintenance.py`，不要在业务路径内联。