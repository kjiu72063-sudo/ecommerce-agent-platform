# 09 - 质量基线与门禁台账

本文件是**带日期的权威质量记录**。凡涉及"当前基线/当前测试数/已合并"的表述，一律以本台账为准，避免 README 与各阶段文档数字漂移。

## 1. 如何验证门禁

在仓库根目录执行：

```bash
uv run pytest -q                          # 全量测试
uv run ruff check src                      # lint（门禁仅覆盖 src）
uv run ruff format --check src             # 格式化
uv run pre-commit run --all-files           # 本地钩子（ruff + pytest）
```

CI 在 `main` 与每个 PR 上强制执行前三条（见 `.github/workflows/ci.yml`）。

## 2. 质量台账

| 日期 | 测试数 | main commit | 门禁 | 说明 |
|---|---|---|---|---|
| 2026-09-12 | 154 | `1fc9cd7` | ✅ ruff/pytest/pre-commit | H1 并发幂等守卫恢复 + M1 失败清理统一；引入 GitHub Actions CI；README 基线修正 |
| 2026-09-12 | 166 | `e870132` | ✅ ruff/pytest/pre-commit | 接线 30 天保留到 CLI；retention 测试重构（端口注入 + SQLite）；补齐 6 个死分支测试 |

> 记录规则：每次合并到 `main` 后，更新测试数、main commit，本表只保留最近若干条，更早的移入「历史基线」。

### 历史基线
- 2026-09-12 之前：README 曾写 52/117/108 等过期数字，均以本台账与实测为准失效。

## 3. 后台维护 / 调度缝

30 天保留归档 `RetentionService.archive_expired` 提供**真实可调度**的两个入口，共用
`src/presale/maintenance.archive_expired_sqlite`：

**HTTP 端点**（供外部调度器触发）：

```http
POST /api/v1/presale/maintenance/retention/archive?tenant_id=<租户>&database=<sqlite路径>
POST /api/v1/presale/maintenance/health                      # 存活探针
```

Cron 约定示例（每日一次）：

```bash
# 每天 02:00 对 tenant-demo 执行保留归档
0 2 * * * curl -sS -X POST \
  "http://localhost:8000/api/v1/presale/maintenance/retention/archive" \
  --data-urlencode "tenant_id=tenant-demo" \
  --data-urlencode "database=/var/lib/presale/presale.sqlite3"
```

**CLI**（适合脚本/手动）：

```bash
uv run python -m presale.cli --archive-expired --db <sqlite路径> --tenant <租户>
```

保留语义：仅归档 `COMPLETE` 且超过 30 天、未归档的 trace；历史 stages 保留，不物理删除；跨租户由 `tenant_id` 隔离。

## 4. 已知限制与待办

- **main 分支保护**：当前 GitHub 免费私有仓返回 403，无法启用 branch protection。升级 Pro 或转公开后应开启：要求 PR、CI 通过后再合并。
- **B4–B6**：Harness、Loop、业务 Agent 尚未实现（见 README 阶段表）。
- chain 任务：新增后台周期任务时应复用 `maintenance.py`，不要在业务路径内联。