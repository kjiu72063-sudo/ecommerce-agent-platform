# 项目领域上下文

> 本文件只记录稳定的领域术语和边界，不记录实现细节。
> 详细模型见 `docs/domain-model.md`，未决选择见 `docs/domain-decisions.md`。

## 系统目的

为电商平台内部商品运营和客服运营团队提供只读、带商品依据、可转人工、可追溯的售前问答 Agent。

## 当前业务范围

V1 只处理售前商品问答：内部操作者提交问题，问题绑定一个明确商品，系统从商品运营团队维护的已发布知识中检索字段/片段级证据，生成回答草稿，并提供人工处置建议。

V1 不执行改价、下单、库存写入、支付、订单操作或站外触达。

## 稳定术语

- **Tenant / 租户**：商品、知识、配置和运行记录的隔离边界。
- **Operator / 内部操作者**：商品运营或客服运营人员。
- **Product / 商品**：售前问题所指向的业务商品。
- **ProductQuestion / 商品问题**：一次需要回答的售前问题。
- **ProductScope / 商品范围**：本次问题允许访问的商品和知识边界。
- **ProductKnowledgeSource / 商品知识来源**：可授权使用的商品资料及其版本和来源定位。
- **EvidenceItem / 证据项**：本次运行实际使用、可定位到来源版本的事实片段。
- **AnswerDraft / 回答草稿**：系统生成、供内部用户审核或处置的回答建议，不等于已发送消息。
- **HumanDisposition / 人工处置**：接受、编辑、转人工或丢弃回答草稿的业务动作。
- **Task**：一次可调度的技术任务，不等于 ProductQuestion。
- **AgentRun**：Task 的一次具体执行尝试。
- **ContextPackage**：一次回答生成所使用的结构化上下文快照。
- **ToolCall**：AgentRun 对已声明工具的一次调用事实。
- **Provenance**：说明数据、上下文或回答事实来自哪个来源版本和位置的信息。
- **Event**：已发生且可追加记录的运行事实。
- **Checkpoint**：AgentRun 在可恢复节点上的执行快照；V1 单次问答中不是核心成功标准。

## 边界规则

1. 事实性回答必须有证据引用，或明确进入人工处置。
2. 一次运行使用的知识版本、配置版本和上下文摘要必须可追溯。
3. V1 ToolCall 只能执行只读知识查询。
4. Task/AgentRun 技术状态不能代替 ProductQuestion 的人工处置状态。
5. Event 和 provenance 记录的是历史事实，不能通过修改当前状态抹除。
6. 置信度是解释性信号，不等同于已验证准确率。
7. 无证据、证据冲突、超商品范围、涉及实时价格/库存、权限失败或系统失败时必须转人工。
8. `accepted` 和 `edited` 只表示内部采用，不表示已发送给消费者；编辑必须保留原始与编辑后版本。
9. V1 原型运行记录固定保留 30 天。
10. “支持实时价格/库存”只有在明确接入权威只读来源并记录快照时间后才成立；V1 当前不接入真实实时系统。
