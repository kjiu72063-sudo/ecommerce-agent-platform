# 有效示例文件完成总结

> 更新日期：2026-09-01
> 状态：15/15 有效示例全部完成 ✅

---

## 已创建的示例文件

### 定义对象示例（8个）

| 文件 | 资源类型 | 核心知识点 |
|---|---|---|
| agent-spec.json | AgentSpec | 引用、预算、权限 |
| skill-manifest.json | SkillManifest | 触发示例、沙箱、评估 |
| tool-manifest.json | ToolManifest | 副作用、幂等、审批 |
| prompt-package.json | PromptPackage | 分层、变量、兼容性 |
| model-policy.json | ModelPolicy | 路由、回退、熔断器 |
| context-policy.json | ContextPolicy | Token 预算、脱敏 |
| loop-profile.json | LoopProfile | 迭代策略、停止条件 |
| permission-profile.json | PermissionProfile | 权限规则、Deny 优先 |

### 运行对象示例（7个）

| 文件 | 资源类型 | 核心知识点 |
|---|---|---|
| task.json | Task | 幂等键、预算、保留策略 |
| agent-run.json | AgentRun | 依赖冻结、租约、模型选择 |
| checkpoint.json | Checkpoint | 副作用账本、恢复令牌 |
| tool-call.json | ToolCall | 幂等键、权限决策 |
| approval.json | Approval | 一次性审批、职责分离 |
| artifact.json | Artifact | 不可变、数据分类、加密 |
| event.json | Event | CloudEvents 兼容、追踪 |

---

## 关键设计总结

### 1. 统一资源信封

```json
{
  "api_version": "agent-platform/v1alpha1",
  "kind": "AgentSpec",
  "metadata": { ... },
  "spec": { ... },
  "status": { ... }
}
```

### 2. 强类型 ID

```
agt_0198f6d0-7ef0-7b0e-a0d3-5f9c96c7f411
↑ 前缀    ↑ UUIDv7（版本号必须是 7）
```

### 3. 精确引用

```json
{
  "kind": "PromptPackage",
  "id": "prm_0198f6d0-...",
  "version": "1.0.0",
  "digest": "sha256:..."
}
```

### 4. 运行对象依赖冻结

```json
"resolved_dependencies": {
  "prompt_package_ref": { ... },
  "context_policy_ref": { ... },
  "model_policy_ref": { ... },
  "loop_profile_ref": { ... },
  "permission_profile_ref": { ... },
  "startup_skill_refs": [...],
  "startup_tool_refs": [...],
  "price_snapshot_ref": { ... },
  "schema_version": "0.2.0",
  "runtime_version": "1.0.0"
}
```

### 5. 权限决策记录

```json
"permission_decision": {
  "decision": "allow",
  "policy_refs": [...],
  "reason_codes": ["RESOURCE_IN_PROJECT_SCOPE"],
  "constraints": { ... },
  "request_digest": "sha256:...",
  "decision_digest": "sha256:..."
}
```

### 6. Event 追踪上下文

```json
"trace": {
  "trace_id": "0198f6d07ef07b0ea0d35f9c96c7f411",
  "span_id": "0198f6d07ef07b0e",
  "correlation_id": "corr_..."
}
```

---

## 下一步

继续创建：
1. 无效示例（19个）
2. 状态机 JSON（6个）
3. 一致性测试（31项）
